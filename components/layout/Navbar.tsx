'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { ShoppingCart, UserCircle, Menu, X, LogIn } from 'lucide-react';
import { useEffect, useState } from 'react';
import { api } from '@/lib/client/api';
import { money } from '@/lib/utils';
import { useAuth } from '@/features/auth/AuthProvider';
import { Button } from '@/components/ui/Button';

export function Navbar(){const {user}=useAuth(); const [balance,setBalance]=useState(0); useEffect(()=>{if(user)api<any>('/api/users/me').then(d=>setBalance(Number(d.balance||0))).catch(()=>setBalance(0));},[user]); const path=usePathname(); const [open,setOpen]=useState(false); const nav=[['Trang chủ','/'],['Shop','/shop'],['ACC','/shop?category=acc'],['FILE','/shop?category=file'],['Nạp tiền','/deposit'],['Đơn hàng','/orders']]; return <header className="sticky top-0 z-40 border-b border-white/8 bg-[#080712]/70 backdrop-blur-2xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3 md:px-6"><Link href="/" className="group flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-linear-to-br from-fuchsia-500 via-violet-500 to-cyan-400 font-black text-white shadow-neon">LV</div><div><div className="text-sm font-black tracking-[.18em] text-white">LÙ A VANG</div><div className="text-[10px] uppercase tracking-[.26em] text-white/35">digital commerce</div></div></Link><nav className="hidden items-center gap-1 lg:flex">{nav.map(([label,href])=><Link key={label} href={href} className={`rounded-lg px-3 py-2 text-sm transition ${path===href?'bg-white/8 text-white':'text-white/55 hover:bg-white/5 hover:text-white'}`}>{label}</Link>)}</nav><div className="hidden items-center gap-2 lg:flex">{user?<><Link href="/cart" className="grid h-10 w-10 place-items-center rounded-xl border border-white/10 bg-white/5 text-white/70 hover:text-white"><ShoppingCart className="h-4 w-4"/></Link><Link href="/profile" className="flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-sm"><UserCircle className="h-4 w-4"/><span className="max-w-28 truncate">{user.displayName||user.email}</span><span className="hidden text-xs text-cyan-200/80 sm:inline">{money(balance)}</span></Link></>:<><Link href="/login" className="inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition duration-200 border border-white/15 bg-white/8 text-white hover:bg-white/12"><LogIn className="h-4 w-4"/>Đăng nhập</Link><Link href="/register" className="inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition duration-200 bg-linear-to-r from-fuchsia-500 via-violet-500 to-cyan-400 text-white shadow-neon hover:brightness-110">Đăng ký</Link></>}</div><button onClick={()=>setOpen(!open)} className="grid h-10 w-10 place-items-center rounded-xl border border-white/10 bg-white/5 lg:hidden">{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t border-white/8 px-4 pb-4 pt-2 lg:hidden">{nav.map(([label,href])=><Link key={label} href={href} onClick={()=>setOpen(false)} className="block rounded-xl px-3 py-3 text-white/75 hover:bg-white/5">{label}</Link>)}{user?<Link href="/profile" className="mt-2 block rounded-xl bg-white/8 px-3 py-3 text-white">Tài khoản</Link>:<div className="mt-2 grid grid-cols-2 gap-2"><Link href="/login" className="inline-flex w-full items-center justify-center rounded-xl border border-white/15 bg-white/8 px-4 py-2.5 text-sm font-semibold text-white hover:bg-white/12">Đăng nhập</Link><Link href="/register" className="inline-flex w-full items-center justify-center rounded-xl bg-linear-to-r from-fuchsia-500 via-violet-500 to-cyan-400 px-4 py-2.5 text-sm font-semibold text-white shadow-neon hover:brightness-110">Đăng ký</Link></div>}</div>}</header>}
