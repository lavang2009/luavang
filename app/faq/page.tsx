import {Card} from '@/components/ui/Card';export default function FAQ(){const faqs=[['Nạp tiền bao lâu?','Chuyển khoản được ghi nhận khi webhook provider được xác thực và đối chiếu đúng mã + số tiền.'],['ACC có được giao ngay?','Đơn dùng ví được xử lý phía server; ACC được lock trước rồi mới tạo delivery.'],['FILE có public không?','Không. File trả phí dùng private Storage path và signed URL ngắn hạn.'],['Có tự động cộng tiền theo frontend không?','Không. Wallet mutation chỉ thực hiện bởi trusted server-side logic.']];return <div className="mx-auto max-w-3xl px-4 py-12 md:px-6"><h1 className="text-4xl font-black">FAQ</h1><div className="mt-8 space-y-3">{faqs.map(([q,a])=><Card key={q} className="p-5"><div className="font-semibold">{q}</div><div className="mt-2 text-sm leading-6 text-white/45">{a}</div></Card>)}</div></div>}
