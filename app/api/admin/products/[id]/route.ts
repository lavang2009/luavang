import {NextRequest} from 'next/server';import {z} from 'zod';import {requireAdmin} from '@/lib/server/auth';import {adminDb} from '@/lib/firebase-admin';import {ok,handleApiError} from '@/lib/server/http';import {safeImageUrl} from '@/lib/utils';
const schema=z.object({price:z.number().int().nonnegative().optional(),originalPrice:z.number().int().nonnegative().optional(),status:z.enum(['active','inactive']).optional(),featured:z.boolean().optional(),description:z.string().max(5000).optional(),thumbnail:z.string().url().refine(v=>safeImageUrl(v)!=='','Thumbnail phải là URL HTTPS an toàn.').optional()});
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{await requireAdmin(req);const {id}=await params;const b=schema.parse(await req.json());const ref=adminDb().collection('products').doc(id);const snap=await ref.get();if(!snap.exists)throw new Error('NOT_FOUND');const current=snap.data()!;const price=b.price??Number(current.price);const original=b.originalPrice??Number(current.originalPrice);const patch:any={...b,discount:original>0?Math.max(0,Math.round((1-price/original)*100)):0,updatedAt:new Date()};await ref.set(patch,{merge:true});return ok({updated:true})}catch(e){return handleApiError(e)}}
export async function DELETE(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{await requireAdmin(req);const {id}=await params;const ref=adminDb().collection('products').doc(id);const snap=await ref.get();if(!snap.exists)throw new Error('NOT_FOUND');await ref.set({status:'inactive',updatedAt:new Date()},{merge:true});return ok({deleted:true,mode:'soft-delete'})}catch(e){return handleApiError(e)}}
